# Pre-built wheels only (no source compilation)
# For HF Spaces: use only binary wheels to avoid timeout during pip install

tensorflow-cpu==2.13.0
cantera==2.6.0; python_version < '3.13'
numpy==1.26.4
matplotlib>=3.7.0
scikit-learn>=1.3.0
networkx>=3.0
pandas>=2.0.0
joblib>=1.3.0
gradio>=4.0.0

# Note: Cantera may not have wheels for Python 3.13+
# If you need Python 3.13 support, see Dockerfile.slim for lightweight approach