# Pre-built wheels only (no source compilation)
# All versions pinned to ensure wheels exist for Python 3.10
# DO NOT use >= or >, only == for exact version matching

tensorflow-cpu==2.13.0
cantera==2.6.0
numpy==1.24.3
matplotlib==3.9.2
pandas==2.2.3
scikit-learn==1.5.2
networkx==3.3
joblib==1.4.2
gradio==4.29.0

# Python 3.10 is required - see runtime.txt
# All versions above have pre-built wheels (.whl) for Python 3.10
